[defaults]
inventory = inventory/hosts
# WARNING: Disable host_key_checking only in trusted/isolated environments.
# In production, set this to True to protect against man-in-the-middle attacks.
host_key_checking = False
retry_files_enabled = False
